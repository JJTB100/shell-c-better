// Consumes Tokens and builds an Abstract Syntax Tree (AST). For a basic shell, this is a linked list of Command structs, but it scales to handle nested logic (&&, ||, subshells) if structured as a tree.