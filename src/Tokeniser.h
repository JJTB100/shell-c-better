// Takes the raw char* and returns an array or linked list of Token structs (e.g., TOKEN_WORD, TOKEN_PIPE, TOKEN_REDIRECT_OUT). Do not evaluate syntax or execute anything here; only group characters.

